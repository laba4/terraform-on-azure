[project]
name = "app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "psycopg[binary]>=3.2",
    "sqlmodel>=0.0.46",
]
[tool.fastapi]
entrypoint = "main:app"
